package list

// TODO(REALMC-7547): refactor app/project flags into a re-usable input struct
const (
	flagApp      = "app"
	flagAppShort = "a"
	flagAppUsage = "this is the --app usage"

	flagProject      = "project"
	flagProjectShort = "p"
	flagProjectUsage = "this is the --project usage"
)
